<SgInputText
  id="demo-validation"
  label="Somente letras"
  validation={(v) =>
    /[^\p{L}\s]/u.test(v)
      ? "Apenas letras e espacos."
      : null
  }
  onValidation={(msg) => console.log(msg)}
  name="validation"
  register={register}
/>
